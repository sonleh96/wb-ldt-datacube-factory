# Full runtime dependencies for the WB LDT Datacube Factory.
# Python 3.11+ is required. pytest is included for the repository test suite.

PyYAML>=6.0
requests>=2.31

# Geospatial and scientific runtime
dask[array]>=2024.7
earthengine-api>=0.1.417
geemap>=0.35
geopandas>=1.0
netCDF4>=1.7
numba>=0.60
numpy>=2.0
pandas>=2.2
pyarrow>=17
pyogrio>=0.10
pyquadkey2>=0.3
rasterio>=1.3
rasterstats>=0.19
rioxarray>=0.17
shapely>=2.0
xarray>=2024.7

# Test suite
pytest>=8
